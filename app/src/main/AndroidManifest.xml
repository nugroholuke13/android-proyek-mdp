<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="com.example.janad.navigation">

    <application
        android:allowBackup="true"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/AppTheme.Dark">
        <activity android:name=".LoginActivity">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
        <activity
            android:name=".MainActivity"
            android:theme="@style/AppTheme" />
        <activity
            android:name=".SignupActivity"
            android:theme="@style/AppTheme.Dark" />
        <activity
            android:name=".HomeAdminActivity"
            android:theme="@style/AppTheme.Dark" />
        <activity
            android:name=".AddDokterActivity"
            android:theme="@style/AppTheme.Dark" />
        <activity
            android:name=".AddSpesialisActivity"
            android:theme="@style/AppTheme.Dark" />
        <activity
            android:name=".VerifikasiDokterActivity"
            android:theme="@style/AppTheme.Dark" />
        <activity
            android:name=".ListDokterActivity"
            android:theme="@style/AppTheme.Dark" />
        <activity
            android:name=".dokterActivity"
            android:theme="@style/AppTheme.Dark" />
        <activity
            android:name=".jadwalDokter"
            android:theme="@style/AppTheme.Dark" />
        <activity
            android:name=".jadwalLiburDokter"
            android:theme="@style/AppTheme.Dark" />
        <activity
            android:name=".pembatalan"
            android:theme="@style/AppTheme.Dark" />
        <activity
            android:name=".jadwalOffline"
            android:theme="@style/AppTheme.Dark" />
        <activity
            android:name=".jadwalLive"
            android:theme="@style/AppTheme.Dark" />
        <activity android:name=".lihatJadwalDokter" />
        <activity android:name=".lihatJadwalLibur"></activity>
    </application>

</manifest>